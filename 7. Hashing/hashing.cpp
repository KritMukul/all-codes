#include<bits/stdc++.h>
using namespace std;

int main(){

    unordered_map<int,int>mp;
    int key, value;
    for(int i=0;i<10;i++) mp[i]++;

    // for(const [key,value] : mp){
        // cout<<key<<" "<<value;
    // }
    
}